spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:15432}/${DB_DATABASE:appdb}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}